# Supabase Tests

This directory stores validation SQL, test harness queries, expected outputs, and deterministic verification routines.

Testing doctrine:
- latest run selectors preferred
- no hardcoded UUIDs unless debugging historical state
- validate before and after mutation
- verify verdict transitions and narrative integrity
